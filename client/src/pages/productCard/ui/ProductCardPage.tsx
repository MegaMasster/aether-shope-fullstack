import { ProductCard } from "@/features/recommendedProducts"

export const ProductCardPage = () => {
    return (
        <div className="flex flex-col items-center">
            <ProductCard />
        </div>
    )
}